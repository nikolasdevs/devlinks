export interface Link {
  id: string;
  platform: string;
  link: string;
}

export interface Profile {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  profileImage: string;
}
